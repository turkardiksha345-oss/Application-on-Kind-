apiVersion: v1
kind: Service
metadata:
  name: student-backend
  namespace: student-registration
spec:
  selector:
    app: student-backend
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  type: ClusterIP
